# =====================================================
# ENVIRONMENT VARIABLES - AWS Processing Service
# =====================================================
# For Consumer + ML Service running on separate EC2 instance
# Update endpoints with your AWS service addresses

# Kafka Configuration (Required)
# Example for AWS MSK: kafka-1.xxxxx.kafka.us-east-1.amazonaws.com:9092,kafka-2.xxxxx.kafka.us-east-1.amazonaws.com:9092
KAFKA_BROKERS=kafka.example.com:9092
KAFKA_TOPIC=raw_posts
CONSUMER_GROUP=social_insight_processor

# PostgreSQL Configuration (Required)
# Example for AWS RDS: social-insight.xxxxx.us-east-1.rds.amazonaws.com
DB_HOST=social-insight.xxxxx.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=change_me_in_production
DB_NAME=social_insight

# Redis Configuration (Required)
# Example for AWS ElastiCache: social-insight.xxxxx.ng.0001.use1.cache.amazonaws.com
REDIS_ADDR=social-insight.xxxxx.ng.0001.use1.cache.amazonaws.com:6379

# ML Service Configuration
ML_PORT=8001
ML_SERVICE_URL=http://localhost:8001

# Environment
ENV=production
DEBUG=false

# Notes:
# - ML_SERVICE_URL should be http://localhost:8001 since it runs in the same container network
# - Use AWS Secrets Manager or Parameter Store for sensitive values
# - Enable SSL/TLS for RDS connection in production
# - Use VPC Security Groups for network isolation between services
# - Monitor with CloudWatch
